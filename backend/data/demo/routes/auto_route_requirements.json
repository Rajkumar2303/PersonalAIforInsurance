{
  "_comment": "ISOLATED DEMO/MOCK ROUTE REQUIREMENTS (Issue #8.5). Loaded only in execution_mode=mock. Mirrors the real requirements format; mock routes additionally require annual_kilometres because the local mock quote site's vehicle page asks for it.",
  "default": [
    "applicant.identity.legal_name",
    "applicant.address.postal_code",
    "product_data.drivers[0].licence.licence_number",
    "product_data.vehicles[0].identity.vin"
  ],
  "per_route": {
    "mock-insurer": [
      "product_data.vehicles[0].use.annual_kilometres"
    ],
    "mock-insurer-broker": [
      "product_data.vehicles[0].use.annual_kilometres"
    ]
  }
}
