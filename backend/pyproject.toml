[project]
name = "allquote-backend"
version = "0.1.0"
description = "Ontario All-Quote Agent backend — evidence-first Ontario auto-insurance shopping assistant"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
addopts = "-q"
