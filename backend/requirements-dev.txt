# Ontario All-Quote Agent — backend development/test dependencies (Issue 1)
# Install: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27

# In-memory SQLite driver for hermetic persistence tests (Issue #10)
aiosqlite>=0.20
