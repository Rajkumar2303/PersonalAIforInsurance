# Learning Journal

A **technical learning journal** for the Ontario All-Quote Agent — one document per
GitHub issue. Each document records what was built, why, how it works, how data
flows through it, the key files/symbols, architectural decisions and tradeoffs,
observability/privacy implications, testing strategy, failure modes, and enough
context to explain or rebuild it.

## Conventions

- One document per issue: `docs/learning/issue-NN-<slug>.md`.
- Create/update a document **after** the issue implementation is complete — it is
  part of the issue's definition of done.
- Base every document on the **code actually implemented** for that issue.
- Clearly distinguish **implemented** behavior, **future planned** behavior, and
  **inferred** tradeoffs.
- Link to earlier documents when a concept depends on prior work; avoid repeating
  explanations already covered.
- Keep the index below up to date whenever a new document is created.

## Index

| Issue | Title | Document | Main concepts | Depends on | Status |
|-------|-------|----------|---------------|------------|--------|
| 1 | Project Setup, Architecture & Observability | [issue-01-foundation.md](./issue-01-foundation.md) | Monorepo; FastAPI factory; Pydantic v2 Settings; LangGraph typed state + 2-node workflow; LangSmith (EU) tracing with request-id ↔ trace-id correlation; structured redacting logging; sensitive-data redaction utility; Playwright foundation; Vite+React health shell; hermetic pytest suite | — | ✅ Implemented (Issue #1) |
| 2 | Canonical Insurance Intake Schema | [issue-02-insurance-schema.md](./issue-02-insurance-schema.md) | Product-aware schema (`InsuranceType` enum; AUTO implemented, others recognized-unsupported); shared vs product-specific composition; nested Pydantic models (drivers, vehicles, household, history, coverage); enums; required-vs-optional + data minimization; postal/VIN/year/percentage validation; sensitive-aware base with leaf-level redaction (`safe_dict`/`redacted_dict`, redacted repr/str); schema versioning; missing-field + trace-metadata helpers | Issue 1 | ✅ Implemented (Issue #2) |
| 3 | Product-Aware Market Registry + Progressive Profile Hardening | [issue-03-market-registry.md](./issue-03-market-registry.md) | Part A: draft profiles (DOB/address optional, zero drivers/vehicles), canonical field paths (`paths.py`), validated immutable `updated(path,value)`, `is_draft`/`is_live_quote_ready`. Part B: data-driven `MarketRegistryEntry` + enums (DistributionType/ProductScope/RegistryStatus/MarketRequirement), AUTO seed from brief Appendix A (31 records, `status:discovered`, nullable rate-source ids), `MarketRegistryService`, read-only `/api/v1/markets`; seeded-vs-verified distinction; no PII | Issues 1, 2 | ✅ Implemented (Issue #3) |
| 4 | Rate Source Deduplication | [issue-04-rate-source-deduplication.md](./issue-04-rate-source-deduplication.md) | Deterministic evidence-based dedup: `DeduplicationStatus`/`Confidence`/`ReasonCode`; authoritative `MarketRegistryEntry.distinct_rate_source_id` (single source of truth) + `DistinctRateSource` records with `related_registry_ids` consistency-checked against the registry; candidate detection vs confirmation; `evaluate_pair` priority chain (id → verified program → underwriter → group → unresolved); same group never auto-collapses; `duplicate_confirmed` suppresses; data-driven mappings; read-only `/api/v1/rate-sources`, `/markets/{id}/duplicates`, `/dedup/metrics`; honest real seed 0/0/31 | Issues 1–3 | ✅ Implemented (Issue #4) |
| 5 | Product-Aware Consent Intake Agent | [issue-05-intake-agent.md](./issue-05-intake-agent.md) | Deterministic progressive intake: product gate (AUTO vs not-implemented); data-driven `IntakeFieldDefinition` catalog (`data/intake/auto_fields.json`) for HOW-TO-ASK while Pydantic validates; `IntakeEngine` (seed bootstrap, `_pick_next_field` unit-blockers∪requested∪starter, `submit_answer`, `request_fields` already_known/requested/unsupported, safe summary); `ProfileVault` Protocol + in-memory + Fernet encrypted-at-rest (key from env, dir gitignored); typed consent (collection/route_disclosure/household_driver) receipts with paths-not-values; route data-sharing preview + exclusion; human checkpoints (signature/payment/purchase must_not_automate); LangGraph advance/submit flows with safe-metadata-only state + pending-answer inbox (no PII in traces); optional `years_at_current_address` (schema 1.0→1.1); dynamic-change Scenarios A–G | Issues 1–4 | ✅ Implemented (Issue #5) |
| 6 | Core Route Planner | [issue-06-route-planner.md](./issue-06-route-planner.md) | Deterministic per-route pre-flight plan (Parts 1 & 2): product-aware AUTO routing (non-AUTO → not-applicable); `RoutePlan`/`PlannedRoute`/`RouteBlocker` (multiple simultaneous blockers); per-route readiness (NOT global `is_live_quote_ready`); registry + Issue #4 dedup integration — confirmed duplicate groups → **primary + visible alternatives** (`is_alternative`); possible/unresolved stay visible; data-driven `RequirementResolver` (`data/routes/auto_route_requirements.json` + deterministic `MarketRequirement`→path map); Issue #5 integration via `RoutePlannerProfileSource`/`field_presence` (presence booleans, never values) + `request_missing_fields` + `field_gate` (household-driver consent → `human_required` blocker); consent integration; route channels online/phone/callback/broker/**human**/**discovery_only**; deterministic ranking; safe LangGraph orchestration (`route_planner_workflow`, metadata-only state); read-only API; 14-scenario hardening suite (`test_route_planner_hardening.py`, 306 tests total) | Issues 1–5 | ✅ Implemented (Issue #6) |
| 7 | Browser Quote Agent | [issue-07-browser-quote-agent.md](./issue-07-browser-quote-agent.md) | Playwright browser execution for READY web routes: route-plan-driven (`planned_route_id` via one centralized compat shim → registry_id); sandbox (local mock quote site, external requests blocked) vs LIVE (personal-use gate + privacy defaults: no video/trace/screenshot/HAR); `BrowserExecutor` generic step (host recheck → consent recheck → detect → checkpoint gate → inspect → map → JIT `get_field_value` fill → pause/navigate → observe); data-driven `BrowserRouteConfig`/`BrowserFieldBinding`/`MatchStrategy`/`FillStrategy`/`TransformKind`; just-in-time ProfileVault boundary (`IntakeEngine.get_field_value` scalar-only); Issue #5 progressive integration (batched missing fields, ask-once, resume same session); conditional fields (re-inspect loop); dynamic field/website changes config-only (label/selector/order/optional→required/removal/type); consent recheck + disclosure-scope expansion + revocation + household-driver gate; ambiguity (field/action), unsupported option, validation-error pauses; CAPTCHA/access-control stop; human checkpoints + prohibited boundaries; allowed-host protection; crash/timeout → safe technical observation; raw `RawQuoteObservation` (annual/monthly, estimate vs firm, private reference handle); callback/manual handoff observation (no call); bounded LangGraph `browser_workflow`; safe API; privacy meta-tests; 140 Issue #7 tests (448 total) | Issues 1, 3, 4, 5, 6 | ✅ Implemented (Issue #7) |
| 8 | Terminal Status & Recovery | [issue-08-terminal-status-recovery.md](./issue-08-terminal-status-recovery.md) | Deterministic recovery/orchestration layer between Issue #7 observations and final coverage outcomes: state separation (RouteReadiness vs BrowserObservation/ExecutionObservation vs AttemptLifecycleStatus vs RouteOutcomeStatus); `ExecutionObservation` generic contract (browser now, voice later); `AttemptRecord`/`RecoveryDecision`; `RecoveryEngine` (consent gate via `IntakeConsentSource`, pause/resume/retry/failover/terminal, `_pick_alternative` no-reuse, `_failover_or_terminal`, `enrich_terminal`); data-driven `RecoveryPolicy` (route 2 / rate-source 3 / plan 6, `data/recovery/auto_policy.json`); `AttemptStore` Protocol + in-memory (Issue #10 replaceable); transition validation (`_ALLOWED_TRANSITIONS`, `TransitionError`); terminal immutability; idempotency (observation key + sequence); stale/out-of-order guard; browser-session-loss handling; readiness + consent recheck; CAPTCHA/auth/prohibited boundaries never retried; `quote_pending_normalization` (comparability deferred); `duplicate_rate_source` for unused alternatives; policy versioning; generic LangGraph `recovery_workflow`; safe-context allowlist (LangSmith); 142 Issue #8 tests (590 total) | Issues 1–7 | ✅ Implemented (Issue #8) |
| 9 | Voice Handoff | [issue-09-voice-handoff.md](./issue-09-voice-handoff.md) | Provider-agnostic deterministic voice/phone layer (Prompts 1 & 2). P1: `VoiceEngine` (single authority), automation disclosure gate, JIT value disclosure via Issue #5 `IntakeValueSource` (never a second applicant store), rule-based `DeterministicBrokerQuestionInterpreter`, human boundaries (identity/declaration/advice/applicant-required) always escalated, quote-vs-estimate preserved, localized Issue #8 classification rows with `source_channel=VOICE`, `voice_workflow` LangGraph, safe API, privacy meta-tests + dynamic fields. P2: **unattended execution** — happy path asserts `applicant_interruptions == 0`; missing fields / human escalation / failures are route-local (never block other routes); missing fields **batched** in `pending_field_paths`; pre-collected consent re-checked JIT, revocation wins immediately; JIT values **discarded after use**; stable `VoiceRouteStatus` contract + `route_summaries` for the future orchestrator; `pause` endpoint (idempotent); browser-callback → Issue #8 → voice integration where the **voice continuation owns its own Issue #8 attempt** (parent = browser attempt) and progresses independently while the browser `callback_required` attempt stays immutable (tests A–E); phone-only-route integration; privacy hardened for email/phone/claim/quote-ref. Never assigns `quoted_comparable`/`quoted_non_comparable`. 89 Issue #9 tests | Issues 1, 8 | ✅ Implemented (Issue #9, Prompts 1 & 2) |
| 10 | Evidence, Audit & Trace Store | [issue-10-evidence-audit.md](./issue-10-evidence-audit.md) | Durable evidence/audit store (Prompts 1 & 2): typed safe payloads; `EvidenceService` (validate → sanitize URL → SHA-256 hash → deterministic idempotency key → persist); repo Protocol + in-memory + SQLAlchemy async (Postgres/SQLite); per-attempt ordering; `QuoteObservation` Decimal money; Alembic migration; safe read/export/health API; **automatic emission** via `EvidenceSink` (recovery decisions/transitions, browser per-step observations + quotes, voice session/observations/quotes, route/attempt lineage, consent, privacy checkpoints); persistence-failure policy (explicit `persistence_failed`, route-local, never retries provider); money Decimal hardening (exact parse from text); skip-gated Postgres integration profile; callback→voice child-attempt lineage; idempotency under auto-emission; concurrency-safe ordering; 102 non-gated + 6 gated tests; never assigns `quoted_comparable`/`quoted_non_comparable` | Issues 1, 7, 8, 9 | ✅ Implemented (Issue #10, Prompts 1 & 2) |
| 11 | Quote Normalization | [issue-11-quote-normalization.md](./issue-11-quote-normalization.md) | Provider-independent canonical quotes (Prompt 1): `NormalizedQuote` + typed `CoverageLedger`; `unknown` first-class (never `excluded`); firm-vs-estimate preserved, estimates never promoted; data-driven `CoverageMappingRegistry` (`data/normalization/auto_coverage_mappings.json`, `rule_version`) — exact leading-phrase matching, no fuzzy matching, no `if registry_id` branching; `PremiumNormalizer` (Decimal; annual or monthly×12 derived; never fabricates for voice); statuses `normalized`/`partially_normalized`/`insufficient_evidence` (never comparable); `NormalizationRepository` (in-memory + SQLAlchemy async) with `normalized_quotes`/`normalized_coverage_items`, idempotent on (source_quote_observation_id, rule_version); `QuoteNormalizationService` consumes Issue #10 `QuoteObservation` (raw evidence immutable); Alembic 0002; read-only `/api/v1/normalized-quotes/*`; Issue #10 extension persisting safe coverage/discount label segments; E2E mock-site normalization; never assigns `quoted_comparable`/`quoted_non_comparable` | Issues 1, 7, 10 | ✅ Implemented (Issue #11, Prompt 1) |
| 12 | Comparability & Confidence | [issue-12-comparability-confidence.md](./issue-12-comparability-confidence.md) | Lite MVP (no scoring): `QuoteComparisonResult` + `QuoteComparisonService` turn Issue #11 `NormalizedQuote`s into `comparable` / `insufficient_coverage_information` / `estimate_only` / `duplicate_rate_source` / `normalization_incomplete` / `coverage_mismatch`; essential coverage = TPL limit + collision + comprehensive deductibles (unknown ⇒ insufficient, never excluded); requested vs quoted coverage mismatch reason codes; confirmed `distinct_rate_source_id` duplicates (one deterministic representative; `duplicate_possible` never auto-duplicate); estimates kept separate; comparable firm quotes sort by annual premium ("lowest among comparable", never "best"); frontend-ready `ComparisonSummary`; `/api/v1/comparisons/plans/{plan_id}` + `/routes/{planned_route_id}` (intake-session-scoped); maps to `quoted_comparable`/`quoted_non_comparable` without touching Issue #11 records; no LLM, no new persistence | Issues 4, 11 | ✅ Implemented (Issue #12, LITE) |
| 13 | Dashboard | [issue-13-dashboard.md](./issue-13-dashboard.md) | Lite multi-source comparison run + results UI: `ComparisonRun` lifecycle (prepared→running→completed/completed_with_partial_results/failed); `ComparisonRunService` coordinates RoutePlanner→Browser→Recovery→Evidence→Normalization→Comparison; bounded concurrency (`COMPARISON_MAX_CONCURRENCY`); per-route isolation (CAPTCHA/exception never stops other routes); **auto-normalization** (closes Issue #11 Prompt 2 gap) then Issue #12 comparison; aggregator/duplicate visible (mock executes aggregator; live never double-submits); demo overlay (Provider A/B quotes, aggregator A duplicate, CAPTCHA, estimate) vs live; `POST/GET /api/v1/comparison-runs` (idempotent, ownership-scoped); frontend `ComparisonResults` polls 1s → progress + results table + summary + "Lowest annual premium among comparable quotes"; no LLM, no real voice, no WebSockets | Issues 6, 7, 8, 10, 11, 12 | ✅ Implemented (Issue #13, LITE) |
| 14 | Reliability & Submission | [issue-14-reliability-submission.md](./issue-14-reliability-submission.md) | LITE end-to-end reliability + submission readiness: **safety timeouts** (`COMPARISON_ROUTE_TIMEOUT_SECONDS` per-route browser-workflow bound → `unavailable`, `COMPARISON_RUN_TIMEOUT_SECONDS` run-level backstop → `unresolved`; timeout wraps only the workflow so `finally: manager.close` aborts in-flight nav — never deadlocks Playwright, no blind retries); **env check** `GET /api/v1/demo/env` (`DemoEnvironmentStatus`: DEMO needs no external credentials; optional/live presence booleans only); **frontend hardening** (`ComparisonResults` stops polling at terminal, survives transient failures, client-side 150s max-wait backstop, timer cleanup, safe fallback "No fully comparable quotes…", distinct-rate-source tooltip + evidence note, Compare button disabled + `useRef` double-click guard); **one-command startup** `scripts/start-demo.ps1`; **redacted demo report** `demos/issue14_demo_report.py` (PII-safe JSON); `README.md` Quick Demo + Mermaid architecture + safety limitations; `docs/demo-script.md` + `docs/submission-checklist.md`; `tests/test_issue14_reliability.py` (golden shape, all-failure honest termination, partial results visible, stuck-route timeout, demo/live isolation ×2, env check, double-start idempotency); structured logging without stack traces for expected outcomes; no new architecture/LLM/live providers/voice/Postgres | All of the above | ✅ Implemented (Issue #14, LITE) | |
| 15 | Provider Onboarding (post-14) | [issue-15-provider-onboarding.md](./issue-15-provider-onboarding.md) | Deterministic provider-onboarding utility (`python -m app.tools.provider_onboarding`): existing-registry-first, public candidate URL → SAFE no-PII inspection (reuses `BrowserManager`+`PageInspector`+`PageDetector`+`GenericQuoteSiteAdapter`) → DRAFT `BrowserRouteConfig` (`backend/data/browser_routes/drafts/`, `last_verified_at=None`, never auto-verified) → human-gated `--approve` (promotes to live config + marks registry verified); deterministic canonical mapping (`map_label`, unknown→`unmapped_field`, never guess); soft bot-block classification (`is_bot_block_text`: "you have been blocked"/Cloudflare) + expanded access-control patterns; 14 hermetic tests (URL/host validation, mapping localization, draft-never-verified, promotion gating, demo-routes-refused, aggregator compat, bot-block regression). SAFE inspection of 3 selected real providers: **square-one OPEN** (quote entry found), **belairdirect + lowestrates-ca (and backup insurancehotline) BOT-BLOCKED**, aviva-direct 404; **no real quote obtained** (needs participant data via frontend + human approval). **Post-15 validation:** first real controlled LIVE run on Square One → **evidence-backed `callback_required` blocker** (no quote; redacted evidence preserved in fixtures + `test_evidence_live_blocker.py`); frontend live banner now data-driven from `/api/v1/markets` ("Verified live route: Sonnet, Square One"); results table shows no-quote routes as "—" + status pills ("Callback required"). **Post-15b Sonnet:** onboarded as a **bounded controlled LIVE route** — `RS-SONNET-AUTO` (Sonnet Insurance Company) mapped only to `sonnet` (never conflated with `RS-ZURICH-AUTO`), complete bounded browser route config v3 (quote/callback/access-control/validation detection, derived non-sensitive count bindings, safe-driving checkbox unbound, no PII), consent + live gates preserved, stops at CAPTCHA/declaration/purchase, premium extracted only when explicit, redacted blockers preserved; `test_sonnet_route.py` (10 hermetic tests) + focused 202-test regression green → **PARTIAL readiness** | Issues 3, 4, 5, 6, 7, 10, 13, 14 | ⚠️ PARTIAL (utility ✅; Square One LIVE blocker preserved ✅; Sonnet onboarded as bounded LIVE route ✅; real multi-source quotes ⏳) |

| 16 | Sonnet Autonomous-First Autofill (post-15) | [issue-16-sonnet-autonomous-autofill.md](./issue-16-sonnet-autonomous-autofill.md) | Makes the Sonnet LIVE route's PRIMARY path **automated Playwright filling** driven by the data-driven `sonnet.json` route config, with the operator as a fallback ONLY on pause: `BrowserFieldBinding.constant_value` (non-PII route constant, Province=Ontario, filled without retrieval, never logged); executor `_fill_known` constant/`collection_length`/JIT precedence; 18 canonical bindings + Continue/Next action binding; VIN binding fixed with `\bVIN\b` word-boundary regex (was matching "Province" → `ambiguous_field`); hermetic Sonnet-shaped mock (`_sonnet_html`) + **multistep SPA mock** (`/sonnet-step/1..3` → quote); **v6 licence-submission checkpoint** — `CheckpointBinding.post_fill_paths` post-fill gate pauses (`paused_human_checkpoint` `identity_lookup`) after filling the licence field but BEFORE the submitting click; explicit participant approval via `BrowserSessionManager.approve_checkpoint` + `POST /browser/sessions/{id}/approve-checkpoint` (must-not-automate never approvable); same `browser_session_id` + `attempt_id` resume; declaration/consent remain pre-fill checkpoints; `_find_safe_action` treats post-fill checkpoint actions as clickable on non-trigger screens; **JIT-path audit** `test_sonnet_jit_paths.py` (all 18 bindings fully-qualified + resolve through production `get_field_value`/`get_collection_length`); missing-value → `paused_needs_field` (no blank fill / no Continue); attestation-gated interactive operator driver `demos/sonnet_live_driver.py` with checkpoint YES/approve prompt; per-action privacy-safe logs/evidence (action + canonical path + status only); flaky whole-record voice privacy scan fixed with allowlist content scan | Issues 7, 10, 15 | ✅ Implemented (post-15 Sonnet phase) + ✅ Pre-live verified (v6) |

> Future-issue titles are provisional (derived from the planned file names) and
> will be finalized when each issue is defined.
